# ML Figures Phase B: Example Catalog Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add three ML paper figure example folders (`ablation-bar`, `benchmark-heatmap`, `run-distribution`) with data, specs, READMEs, gallery PNGs, docs updates, and tests — using only the existing `vega-paper infer` command.

**Architecture:** Each example folder follows the established `examples/<name>/` convention: small committed CSV(s), committed reference `.vl.json` spec(s), and a README with copy-paste CLI commands. Specs that `infer` can produce are generated via new root `package.json` `infer:*` scripts (chained into `infer:examples`); figures that need layers (`chart-labeled.vl.json`, `chart-points.vl.json`) are hand-written Vega-Lite v6 specs because the `template` command is Phase C. Gallery PNGs come from `scripts/render-gallery.ts`.

**Tech Stack:** Bun 1.3.14 workspace, TypeScript, `bun test`, biome (`bun run check`), Vega-Lite v6.

**Spec:** [docs/vega-paper-ml-conference-figures-spec.md](../../vega-paper-ml-conference-figures-spec.md) — Phase B (§12), driven by §7.2, §7.3, §7.7, §9.3, §11.

## Global Constraints

- Run every command from the repo root: `/Users/ryusei0623/projects/vega-paper`.
- **No new CLI features.** Only the existing `infer` and `render` commands are used (invoked as `bun run packages/cli/src/index.ts ...` in scripts, matching existing `infer:*` script style).
- Generated and hand-written specs use schema `https://vega.github.io/schema/vega-lite/v6.json`. `infer` defaults are width `360`, height `240` (`packages/cli/src/core/infer.ts` `DEFAULT_WIDTH`/`DEFAULT_HEIGHT`); hand-written specs must match the dimensions of their `infer` sibling.
- Hand-written JSON uses 2-space indentation (biome ignores `examples/**/*.vl.json`, `docs/`, and `skills/`, but stay consistent anyway).
- `examples/boxplot/` is kept **unchanged** (spec §11: `run-distribution/` builds on it and cross-links rather than replacing it).
- Spec §7.2 "Template Extension" bullets and §7.3 `vega-paper template` command are **Phase C — excluded**. Where the spec mentions templates, READMEs may only say the template is "planned".
- Every new `infer:*` script must also be appended to the `infer:examples` chain in `package.json` (spec §13: `bun run infer:examples` regenerates all infer-based examples).
- Hand-written specs (`chart-labeled.vl.json`, `chart-points.vl.json`) are NOT outputs of any `infer:*` script and must never be overwritten by `bun run infer:examples`.
- Gallery `.meta.json` sidecars under `docs/assets/gallery/` are gitignored — commit only the `.png` files.
- Commit messages: `feat: ...` / `docs: ...` style (scoped forms like `feat(examples): ...` are used in this repo).

## File Map

| File | Change |
|------|--------|
| `examples/ablation-bar/{README.md,data.csv,grouped.csv,chart.vl.json,chart-grouped.vl.json}` | Create (Task 1) |
| `examples/benchmark-heatmap/{README.md,data.csv,chart.vl.json,chart-labeled.vl.json}` | Create (Task 2) |
| `examples/run-distribution/{README.md,data.csv,chart-boxplot.vl.json,chart-points.vl.json}` | Create (Task 3) |
| `package.json` | Add 4 `infer:*` scripts; extend `infer:examples` chain (Tasks 1–3) |
| `packages/cli/test/examples.test.ts` | Add 7 tests (Tasks 1–3) |
| `skills/vega-paper/references/chart-selection.md` | Fix "no dedicated bar example" note; add 3 example rows (Tasks 1–3) |
| `scripts/render-gallery.ts` | Add 3 gallery jobs (Task 4) |
| `scripts/render-gallery.test.ts` | Add 3 names to `EXAMPLE_PREVIEWS` (Task 4) |
| `docs/assets/gallery/examples/{ablation-bar,benchmark-heatmap,run-distribution}.png` | Create via `bun run render:gallery` (Task 4) |
| `examples/README.md` | Add 3 rows to index table and gallery table (Task 4) |

**Not changed (deliberately):**
- `examples/boxplot/` — spec §11 says keep as-is; `run-distribution/README.md` cross-links to it.
- Root `README.md` — it links to `examples/README.md` but does not list individual examples, and the §9.3 gallery split into "theme previews + ML recipes" needs Phase C/D figures (Pareto, scaling law, calibration, multipanel) to exist first. Deferred.

---

### Task 1: `examples/ablation-bar/` (spec §7.2)

**Files:**
- Create: `examples/ablation-bar/data.csv`
- Create: `examples/ablation-bar/grouped.csv`
- Create: `examples/ablation-bar/chart.vl.json` (generated by `bun run infer:ablation-bar`)
- Create: `examples/ablation-bar/chart-grouped.vl.json` (generated by `bun run infer:ablation-bar-grouped`)
- Create: `examples/ablation-bar/README.md`
- Modify: `package.json` (scripts block, around lines 19–28)
- Modify: `skills/vega-paper/references/chart-selection.md` (line 18 note; "Examples in this repo" table)
- Test: `packages/cli/test/examples.test.ts`

**Interfaces:**
- Produces: `examples/ablation-bar/chart.vl.json` and `chart-grouped.vl.json` (consumed by Task 4's gallery job and README rows); package.json scripts `infer:ablation-bar`, `infer:ablation-bar-grouped`.

- [ ] **Step 1: Write the failing tests**

Append inside the `describe("examples", ...)` block in `packages/cli/test/examples.test.ts`, immediately before the final closing `});`:

```ts
  test("ablation-bar chart is a bar chart of score by component and method", async () => {
    const spec = await readExampleSpec("examples/ablation-bar/chart.vl.json");

    expect(spec.data).toEqual({ url: "data.csv" });
    expect(spec.mark).toBe("bar");
    expect(spec.encoding).toMatchObject({
      x: { field: "component", type: "nominal" },
      y: { field: "score", type: "quantitative" },
      color: { field: "method", type: "nominal" },
    });
    expect(spec.width).toBe(420);
    expect(spec.height).toBe(240);
  });

  test("ablation-bar grouped chart compares methods across datasets", async () => {
    const spec = await readExampleSpec("examples/ablation-bar/chart-grouped.vl.json");

    expect(spec.data).toEqual({ url: "grouped.csv" });
    expect(spec.mark).toBe("bar");
    expect(spec.encoding).toMatchObject({
      x: { field: "dataset", type: "nominal" },
      y: { field: "score", type: "quantitative" },
      color: { field: "method", type: "nominal" },
    });
  });
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `bun test packages/cli/test/examples.test.ts`
Expected: the two new `ablation-bar` tests FAIL (the `.vl.json` files do not exist yet — `Bun.file(...).json()` throws ENOENT); all pre-existing tests still PASS.

- [ ] **Step 3: Create `examples/ablation-bar/data.csv`**

Exact spec §7.2 schema (component ablation):

```csv
method,component,score,stderr
baseline,none,81.2,0.4
ours,no retrieval,83.1,0.3
ours,no reranking,84.0,0.2
ours,full,85.6,0.2
```

- [ ] **Step 4: Create `examples/ablation-bar/grouped.csv`**

Spec §7.2 grouped schema, extended to four datasets so the grouped figure is meaningful:

```csv
dataset,method,score,stderr
MMLU,baseline,68.2,0.6
MMLU,ours,72.4,0.5
GSM8K,baseline,56.1,1.0
GSM8K,ours,61.8,0.9
HellaSwag,baseline,74.5,0.4
HellaSwag,ours,78.9,0.4
ARC,baseline,62.3,0.8
ARC,ours,66.0,0.7
```

- [ ] **Step 5: Add the infer scripts to `package.json`**

In the root `package.json` scripts block, insert these two lines directly after the `"infer:embedding-scatter"` line:

```json
    "infer:ablation-bar": "bun run packages/cli/src/index.ts infer examples/ablation-bar/data.csv --chart bar --x component --y score --color method --title \"Ablation study\" --width 420 --height 240 --spec-out examples/ablation-bar/chart.vl.json",
    "infer:ablation-bar-grouped": "bun run packages/cli/src/index.ts infer examples/ablation-bar/grouped.csv --chart bar --x dataset --y score --color method --title \"Accuracy by dataset\" --width 420 --height 240 --spec-out examples/ablation-bar/chart-grouped.vl.json",
```

And replace the `"infer:examples"` value so the chain ends with the two new scripts:

```json
    "infer:examples": "bun run infer:training-curve && bun run infer:training-curve-aggregate && bun run infer:training-curve-error-band && bun run infer:confusion-matrix && bun run infer:confusion-matrix-trials && bun run infer:faceted-training && bun run infer:boxplot && bun run infer:boxplot-by-split && bun run infer:embedding-scatter && bun run infer:ablation-bar && bun run infer:ablation-bar-grouped",
```

- [ ] **Step 6: Generate the specs**

Run: `bun run infer:ablation-bar && bun run infer:ablation-bar-grouped`
Expected: exit 0; stdout reports writing `examples/ablation-bar/chart.vl.json` and `examples/ablation-bar/chart-grouped.vl.json`.

The generated `chart.vl.json` must be (2-space JSON, same shape as `examples/boxplot/chart.vl.json`):

```json
{
  "$schema": "https://vega.github.io/schema/vega-lite/v6.json",
  "data": {
    "url": "data.csv"
  },
  "mark": "bar",
  "width": 420,
  "height": 240,
  "encoding": {
    "x": {
      "field": "component",
      "type": "nominal"
    },
    "y": {
      "field": "score",
      "type": "quantitative"
    },
    "color": {
      "field": "method",
      "type": "nominal"
    }
  },
  "title": "Ablation study"
}
```

And `chart-grouped.vl.json`:

```json
{
  "$schema": "https://vega.github.io/schema/vega-lite/v6.json",
  "data": {
    "url": "grouped.csv"
  },
  "mark": "bar",
  "width": 420,
  "height": 240,
  "encoding": {
    "x": {
      "field": "dataset",
      "type": "nominal"
    },
    "y": {
      "field": "score",
      "type": "quantitative"
    },
    "color": {
      "field": "method",
      "type": "nominal"
    }
  },
  "title": "Accuracy by dataset"
}
```

If the generated files differ structurally from the above (beyond whitespace), stop and debug the CLI invocation — do not hand-edit the generated files.

- [ ] **Step 7: Run tests to verify they pass**

Run: `bun test packages/cli/test/examples.test.ts`
Expected: PASS (all tests).

- [ ] **Step 8: Write `examples/ablation-bar/README.md`**

Complete contents:

```markdown
# Ablation bar (infer)

Bar charts for component ablations and benchmark leaderboards — one of the most common ML paper figure types. Two datasets are included:

- `data.csv` — component ablation (`method,component,score,stderr`)
- `grouped.csv` — method comparison across datasets (`dataset,method,score,stderr`)

## Ablation vs leaderboard

- **Ablation:** one system, several variants with a component removed or changed. Put the varied `component` on `--x` and keep the baseline visible for reference. Answers "which parts matter?".
- **Leaderboard / benchmark comparison:** several systems evaluated on shared datasets or tasks. Put the `dataset` on `--x` and the `method` on `--color`. Answers "which system wins where?".

Both use `--chart bar`; only the field roles change.

## Ablation study

```bash
vega-paper infer examples/ablation-bar/data.csv \
  --chart bar \
  --x component \
  --y score \
  --color method \
  --title "Ablation study" \
  --width 420 \
  --height 240 \
  --spec-out examples/ablation-bar/chart.vl.json
```

## Grouped benchmark bar

```bash
vega-paper infer examples/ablation-bar/grouped.csv \
  --chart bar \
  --x dataset \
  --y score \
  --color method \
  --title "Accuracy by dataset" \
  --width 420 \
  --height 240 \
  --spec-out examples/ablation-bar/chart-grouped.vl.json
```

Vega-Lite stacks bars that share an x position, so this variant renders methods stacked per dataset — fine for quick drafts, but stacked scores read as totals. For side-by-side comparison, facet instead (`--x method --facet dataset`, drop `--color`) or wait for the planned `leaderboard-bar` template.

## Render

```bash
vega-paper render examples/ablation-bar/chart.vl.json \
  --theme paper-clean \
  --format svg \
  --out examples/ablation-bar/output.svg
```

The `stderr` column is not used by `infer` yet; value labels, error bars, sorting, and best-method highlighting are planned as a `leaderboard-bar` template.

Committed `.vl.json` files are regenerated with `bun run infer:ablation-bar` and `bun run infer:ablation-bar-grouped` from the repo root. `output.svg` is local only.
```

- [ ] **Step 9: Update `skills/vega-paper/references/chart-selection.md`**

Edit 1 — replace line 18 (spec §7.2 acceptance: no longer says bar has no dedicated example):

Old:

```markdown
There is no dedicated `examples/` folder for `bar` or `area` yet; `scatter` is covered by [embedding-scatter/](../../../examples/embedding-scatter/).
```

New:

```markdown
There is no dedicated `examples/` folder for `area` yet; `bar` is covered by [ablation-bar/](../../../examples/ablation-bar/) and `scatter` by [embedding-scatter/](../../../examples/embedding-scatter/).
```

Edit 2 — in the "Examples in this repo" table, insert directly after the `embedding-scatter` row (`| [embedding-scatter/](../../../examples/embedding-scatter/) | \`scatter\` | \`--color\` for class/cluster labels |`):

```markdown
| [ablation-bar/](../../../examples/ablation-bar/) | `bar` | ablation and grouped benchmark comparison with `--color` |
```

- [ ] **Step 10: Verify formatting and full test file**

Run: `bun run check && bun test packages/cli/test/examples.test.ts`
Expected: biome reports no errors; all tests PASS. (If biome complains about `package.json` or test formatting, run `bun run check:fix` and re-run.)

- [ ] **Step 11: Commit**

```bash
git add examples/ablation-bar package.json packages/cli/test/examples.test.ts skills/vega-paper/references/chart-selection.md
git commit -m "feat(examples): add ablation-bar example for ML ablation and leaderboard bars"
```

---

### Task 2: `examples/benchmark-heatmap/` (spec §7.3)

**Files:**
- Create: `examples/benchmark-heatmap/data.csv` (4 models × 5 tasks — acceptance criterion)
- Create: `examples/benchmark-heatmap/chart.vl.json` (generated by `bun run infer:benchmark-heatmap`)
- Create: `examples/benchmark-heatmap/chart-labeled.vl.json` (hand-written layered spec)
- Create: `examples/benchmark-heatmap/README.md`
- Modify: `package.json`
- Modify: `skills/vega-paper/references/chart-selection.md` ("Examples in this repo" table)
- Test: `packages/cli/test/examples.test.ts`

**Interfaces:**
- Produces: `examples/benchmark-heatmap/chart.vl.json` and `chart-labeled.vl.json` (Task 4 renders `chart-labeled.vl.json` for the gallery); package.json script `infer:benchmark-heatmap`.

> **Cross-plan ownership note:** Phase B owns `examples/benchmark-heatmap/data.csv`, `README.md`, `chart.vl.json`, and `chart-labeled.vl.json`. The Phase C plan's Task 10 currently also writes `data.csv` (with different task columns/scores), `README.md`, and a template-generated `chart-labeled.vl.json` (3 layers with best-cell outline) — when Phase C executes after this plan it must NOT overwrite these Phase-B files with different content; it must reuse this `data.csv`, extend (not replace) this README, and if it regenerates `chart-labeled.vl.json` via the template it must update the layered-spec test in `packages/cli/test/examples.test.ts` (which expects exactly 2 layers, see Step 1) in the same commit. See "Spec Ambiguities Resolved" item 7.

- [ ] **Step 1: Write the failing tests**

Append inside the `describe("examples", ...)` block in `packages/cli/test/examples.test.ts`, before the final closing `});`:

```ts
  test("benchmark-heatmap chart is a rect heatmap of scores", async () => {
    const spec = await readExampleSpec("examples/benchmark-heatmap/chart.vl.json");

    expect(spec.data).toEqual({ url: "data.csv" });
    expect(spec.mark).toBe("rect");
    expect(spec.encoding).toMatchObject({
      x: { field: "task", type: "ordinal" },
      y: { field: "model", type: "ordinal" },
      color: { field: "score", type: "quantitative" },
    });
  });

  test("benchmark-heatmap labeled chart layers rect and text marks", async () => {
    const spec = await readExampleSpec("examples/benchmark-heatmap/chart-labeled.vl.json");

    expect(spec.data).toEqual({ url: "data.csv" });
    const layers = spec.layer as Record<string, unknown>[];
    expect(layers).toHaveLength(2);
    expect(layers[0]).toMatchObject({
      mark: "rect",
      encoding: { color: { field: "score", type: "quantitative" } },
    });
    expect(layers[1]).toMatchObject({
      mark: { type: "text" },
      encoding: { text: { field: "score", type: "quantitative" } },
    });
  });

  test("benchmark-heatmap data covers at least 4 models and 5 tasks", async () => {
    const csv = await Bun.file(join(REPO_ROOT, "examples/benchmark-heatmap/data.csv")).text();
    const rows = csv
      .trim()
      .split("\n")
      .slice(1)
      .map((line) => line.split(","));

    expect(new Set(rows.map((row) => row[0])).size).toBeGreaterThanOrEqual(4);
    expect(new Set(rows.map((row) => row[1])).size).toBeGreaterThanOrEqual(5);
  });
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `bun test packages/cli/test/examples.test.ts`
Expected: the three new `benchmark-heatmap` tests FAIL (missing files); everything else PASSES.

- [ ] **Step 3: Create `examples/benchmark-heatmap/data.csv`**

4 models × 5 tasks (spec §7.3 acceptance criterion), spec-schema `model,task,score`:

```csv
model,task,score
Baseline,MMLU,68.2
Baseline,GSM8K,56.1
Baseline,HellaSwag,74.5
Baseline,ARC,62.3
Baseline,HumanEval,31.7
SFT,MMLU,70.9
SFT,GSM8K,58.8
SFT,HellaSwag,76.2
SFT,ARC,64.1
SFT,HumanEval,38.4
RLHF,MMLU,71.6
RLHF,GSM8K,60.3
RLHF,HellaSwag,77.8
RLHF,ARC,65.0
RLHF,HumanEval,41.2
Ours,MMLU,72.4
Ours,GSM8K,61.8
Ours,HellaSwag,79.1
Ours,ARC,66.5
Ours,HumanEval,44.6
```

- [ ] **Step 4: Add the infer script to `package.json`**

Insert after the `"infer:ablation-bar-grouped"` line:

```json
    "infer:benchmark-heatmap": "bun run packages/cli/src/index.ts infer examples/benchmark-heatmap/data.csv --chart heatmap --x task --y model --color score --title \"Benchmark results\" --width 420 --height 240 --spec-out examples/benchmark-heatmap/chart.vl.json",
```

And append `&& bun run infer:benchmark-heatmap` to the `"infer:examples"` chain, so it ends:

```text
... && bun run infer:ablation-bar && bun run infer:ablation-bar-grouped && bun run infer:benchmark-heatmap
```

- [ ] **Step 5: Generate the infer spec**

Run: `bun run infer:benchmark-heatmap`
Expected: exit 0; writes `examples/benchmark-heatmap/chart.vl.json` with this content:

```json
{
  "$schema": "https://vega.github.io/schema/vega-lite/v6.json",
  "data": {
    "url": "data.csv"
  },
  "mark": "rect",
  "width": 420,
  "height": 240,
  "encoding": {
    "x": {
      "field": "task",
      "type": "ordinal"
    },
    "y": {
      "field": "model",
      "type": "ordinal"
    },
    "color": {
      "field": "score",
      "type": "quantitative"
    }
  },
  "title": "Benchmark results"
}
```

- [ ] **Step 6: Write the hand-written labeled spec**

The `template benchmark-heatmap` command does not exist yet (Phase C); spec §7.3 acceptance allows a committed hand-written Vega-Lite spec instead. Create `examples/benchmark-heatmap/chart-labeled.vl.json` with exactly:

```json
{
  "$schema": "https://vega.github.io/schema/vega-lite/v6.json",
  "data": {
    "url": "data.csv"
  },
  "width": 420,
  "height": 240,
  "title": "Benchmark results",
  "encoding": {
    "x": {
      "field": "task",
      "type": "ordinal"
    },
    "y": {
      "field": "model",
      "type": "ordinal"
    }
  },
  "layer": [
    {
      "mark": "rect",
      "encoding": {
        "color": {
          "field": "score",
          "type": "quantitative"
        }
      }
    },
    {
      "mark": {
        "type": "text",
        "fontSize": 10
      },
      "encoding": {
        "text": {
          "field": "score",
          "type": "quantitative",
          "format": ".1f"
        },
        "color": {
          "condition": {
            "test": "datum.score > 70",
            "value": "white"
          },
          "value": "black"
        }
      }
    }
  ]
}
```

Notes on the shape (valid Vega-Lite v6): shared `x`/`y` encodings sit at the top level of the layered spec; the `rect` layer carries the color scale; the `text` layer prints the score with `.1f` formatting and flips label color to white on dark (high-score) cells via a `condition`.

- [ ] **Step 7: Verify the labeled spec renders**

Run: `bun run packages/cli/src/index.ts render examples/benchmark-heatmap/chart-labeled.vl.json --theme paper-clean --format svg --out examples/benchmark-heatmap/output-labeled.svg`
Expected: exit 0; stdout includes `Rendered examples/benchmark-heatmap/output-labeled.svg` followed by `Wrote examples/benchmark-heatmap/output-labeled.meta.json`. The SVG contains 20 cells with numeric labels. Do not commit these outputs — note that `.gitignore` only ignores the exact name `examples/**/output.svg`, so `output-labeled.svg` and its `.meta.json` sidecar show up as untracked and MUST be deleted after inspection: `rm examples/benchmark-heatmap/output-labeled.svg examples/benchmark-heatmap/output-labeled.meta.json`.

- [ ] **Step 8: Run tests to verify they pass**

Run: `bun test packages/cli/test/examples.test.ts`
Expected: PASS (all tests).

- [ ] **Step 9: Write `examples/benchmark-heatmap/README.md`**

Complete contents:

```markdown
# Benchmark heatmap (infer + hand-written labels)

Model × task score matrix (`model,task,score`) — the figure equivalent of a benchmark results table. The sample data covers 4 models × 5 tasks.

## When to prefer a heatmap over a table

- Use a **heatmap** when the point is the *pattern*: which model dominates, which tasks are hard, where a method helps or hurts. Color makes row/column structure visible at a glance.
- Use a **table** when readers must cite *exact* numbers, when metrics have mixed scales, or when significance markers matter more than trends.
- The labeled variant below is the middle ground: color for pattern, text for exact scores. Prefer it while the grid stays small (roughly up to 10 × 10 cells).

## Basic heatmap (infer)

```bash
vega-paper infer examples/benchmark-heatmap/data.csv \
  --chart heatmap \
  --x task \
  --y model \
  --color score \
  --title "Benchmark results" \
  --width 420 \
  --height 240 \
  --spec-out examples/benchmark-heatmap/chart.vl.json
```

## Labeled heatmap (hand-written spec)

`chart-labeled.vl.json` is a hand-written layered Vega-Lite spec — a `rect` color layer plus a `text` label layer — because `infer` generates single-mark charts only. Render it directly:

```bash
vega-paper render examples/benchmark-heatmap/chart-labeled.vl.json \
  --theme paper-clean \
  --format svg \
  --out examples/benchmark-heatmap/output-labeled.svg
```

Label color flips to white on dark cells via a `condition` on the score; adjust the `datum.score > 70` threshold to your score range when reusing the spec. A `benchmark-heatmap` template command that generates this shape (plus best-score highlighting) is planned.

## Render / lint

```bash
vega-paper render examples/benchmark-heatmap/chart.vl.json \
  --theme paper-clean \
  --format svg \
  --out examples/benchmark-heatmap/output.svg

vega-paper lint examples/benchmark-heatmap/chart.vl.json --profile paper
```

`chart.vl.json` is regenerated with `bun run infer:benchmark-heatmap` from the repo root. `chart-labeled.vl.json` is hand-written and NOT overwritten by `bun run infer:examples`. `output*.svg` files are local only.
```

- [ ] **Step 10: Update `skills/vega-paper/references/chart-selection.md`**

In the "Examples in this repo" table, insert directly after the `ablation-bar` row added in Task 1:

```markdown
| [benchmark-heatmap/](../../../examples/benchmark-heatmap/) | `heatmap` | model × task scores; hand-written labeled variant (rect + text) |
```

- [ ] **Step 11: Verify formatting**

Run: `bun run check && bun test packages/cli/test/examples.test.ts`
Expected: no biome errors; all tests PASS.

- [ ] **Step 12: Commit**

```bash
git add examples/benchmark-heatmap package.json packages/cli/test/examples.test.ts skills/vega-paper/references/chart-selection.md
git commit -m "feat(examples): add benchmark-heatmap example with hand-written labeled variant"
```

---

### Task 3: `examples/run-distribution/` (spec §7.7 Phase 1)

**Files:**
- Create: `examples/run-distribution/data.csv`
- Create: `examples/run-distribution/chart-boxplot.vl.json` (generated by `bun run infer:run-distribution-boxplot`)
- Create: `examples/run-distribution/chart-points.vl.json` (hand-written layered spec)
- Create: `examples/run-distribution/README.md`
- Modify: `package.json`
- Modify: `skills/vega-paper/references/chart-selection.md` ("Examples in this repo" table)
- Test: `packages/cli/test/examples.test.ts`

**Interfaces:**
- Produces: `examples/run-distribution/chart-boxplot.vl.json` and `chart-points.vl.json` (Task 4 renders `chart-points.vl.json` for the gallery); package.json script `infer:run-distribution-boxplot`.
- Constraint: `examples/boxplot/` files are NOT modified; the README cross-links to `../boxplot/`.

- [ ] **Step 1: Write the failing tests**

Append inside the `describe("examples", ...)` block in `packages/cli/test/examples.test.ts`, before the final closing `});`:

```ts
  test("run-distribution boxplot chart summarizes score by method", async () => {
    const spec = await readExampleSpec("examples/run-distribution/chart-boxplot.vl.json");

    expect(spec.data).toEqual({ url: "data.csv" });
    expect(spec.mark).toBe("boxplot");
    expect(spec.encoding).toEqual({
      x: { field: "method", type: "nominal" },
      y: {
        field: "score",
        type: "quantitative",
        scale: { zero: false },
      },
    });
  });

  test("run-distribution points chart layers boxplot and jittered raw points", async () => {
    const spec = await readExampleSpec("examples/run-distribution/chart-points.vl.json");

    expect(spec.data).toEqual({ url: "data.csv" });
    const layers = spec.layer as Record<string, unknown>[];
    expect(layers).toHaveLength(2);
    expect(layers[0]).toMatchObject({ mark: { type: "boxplot" } });
    expect(layers[1]).toMatchObject({
      mark: { type: "point" },
      encoding: { xOffset: { field: "jitter", type: "quantitative" } },
    });
  });
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `bun test packages/cli/test/examples.test.ts`
Expected: the two new `run-distribution` tests FAIL (missing files); everything else PASSES.

- [ ] **Step 3: Create `examples/run-distribution/data.csv`**

Spec §7.7 schema (`method,seed,score`), 5 seeds per method:

```csv
method,seed,score
baseline,1,80.1
baseline,2,79.4
baseline,3,80.6
baseline,4,79.9
baseline,5,80.3
ours,1,83.2
ours,2,84.0
ours,3,82.7
ours,4,83.6
ours,5,84.3
```

- [ ] **Step 4: Add the infer script to `package.json`**

Insert after the `"infer:benchmark-heatmap"` line:

```json
    "infer:run-distribution-boxplot": "bun run packages/cli/src/index.ts infer examples/run-distribution/data.csv --chart boxplot --x method --y score --title \"Score distribution over seeds\" --spec-out examples/run-distribution/chart-boxplot.vl.json",
```

And append `&& bun run infer:run-distribution-boxplot` to the `"infer:examples"` chain. The final full chain value is:

```json
    "infer:examples": "bun run infer:training-curve && bun run infer:training-curve-aggregate && bun run infer:training-curve-error-band && bun run infer:confusion-matrix && bun run infer:confusion-matrix-trials && bun run infer:faceted-training && bun run infer:boxplot && bun run infer:boxplot-by-split && bun run infer:embedding-scatter && bun run infer:ablation-bar && bun run infer:ablation-bar-grouped && bun run infer:benchmark-heatmap && bun run infer:run-distribution-boxplot",
```

- [ ] **Step 5: Generate the boxplot spec**

Run: `bun run infer:run-distribution-boxplot`
Expected: exit 0; writes `examples/run-distribution/chart-boxplot.vl.json` with this content (default 360×240 — no `--width`/`--height` passed, matching `examples/boxplot/`):

```json
{
  "$schema": "https://vega.github.io/schema/vega-lite/v6.json",
  "data": {
    "url": "data.csv"
  },
  "mark": "boxplot",
  "width": 360,
  "height": 240,
  "encoding": {
    "x": {
      "field": "method",
      "type": "nominal"
    },
    "y": {
      "field": "score",
      "type": "quantitative",
      "scale": {
        "zero": false
      }
    }
  },
  "title": "Score distribution over seeds"
}
```

- [ ] **Step 6: Write the hand-written overlay spec**

Create `examples/run-distribution/chart-points.vl.json` with exactly:

```json
{
  "$schema": "https://vega.github.io/schema/vega-lite/v6.json",
  "data": {
    "url": "data.csv"
  },
  "width": 360,
  "height": 240,
  "title": "Score distribution over seeds",
  "encoding": {
    "x": {
      "field": "method",
      "type": "nominal"
    },
    "y": {
      "field": "score",
      "type": "quantitative",
      "scale": {
        "zero": false
      }
    }
  },
  "layer": [
    {
      "mark": {
        "type": "boxplot",
        "opacity": 0.5
      }
    },
    {
      "transform": [
        {
          "calculate": "datum.seed / 6",
          "as": "jitter"
        }
      ],
      "mark": {
        "type": "point",
        "filled": true,
        "size": 40,
        "opacity": 0.9
      },
      "encoding": {
        "xOffset": {
          "field": "jitter",
          "type": "quantitative"
        }
      }
    }
  ]
}
```

Notes on the shape (valid Vega-Lite v6): this follows the documented Vega-Lite jitter pattern (`xOffset` with a quantitative field spreads points within the x band; adding `xOffset` switches x to a band scale, which the layered `boxplot` composite mark also uses). The jitter is computed deterministically from the `seed` column (`datum.seed / 6` maps seeds 1–5 into the band) instead of `random()`, so re-renders are reproducible. The layer-level `transform` keeps the jitter field out of the boxplot layer.

- [ ] **Step 7: Verify the overlay spec renders**

Run: `bun run packages/cli/src/index.ts render examples/run-distribution/chart-points.vl.json --theme paper-clean --format svg --out examples/run-distribution/output-points.svg`
Expected: exit 0; stdout includes `Rendered examples/run-distribution/output-points.svg` followed by `Wrote examples/run-distribution/output-points.meta.json`. The SVG shows two translucent boxes with 5 visible points spread horizontally inside each. `output-points.svg` is NOT covered by `.gitignore` (which only ignores the exact name `output.svg`), so clean up: `rm examples/run-distribution/output-points.svg examples/run-distribution/output-points.meta.json`.

- [ ] **Step 8: Run tests to verify they pass**

Run: `bun test packages/cli/test/examples.test.ts`
Expected: PASS (all tests).

- [ ] **Step 9: Write `examples/run-distribution/README.md`**

Complete contents:

```markdown
# Run distribution (infer + hand-written overlay)

Score variability over random seeds (`method,seed,score`). ML papers report this to show that an improvement is not a lucky seed. This recipe builds on the minimal [../boxplot/](../boxplot/) example and reframes it for seed/run distributions.

## Boxplot vs raw-point overlay

- **Boxplot only** (`chart-boxplot.vl.json`): fine when each group has enough samples (roughly 10+) for quartiles to be meaningful, or when there are too many groups to show individual points.
- **Boxplot + raw points** (`chart-points.vl.json`): prefer this for the typical 3–10 seeds per method. Quartile summaries of so few samples hide the actual runs; the overlay keeps the summary while showing every seed.
- **Histogram / density:** better for hundreds of runs; planned as future `infer` chart types.

## Boxplot over seeds (infer)

```bash
vega-paper infer examples/run-distribution/data.csv \
  --chart boxplot \
  --x method \
  --y score \
  --title "Score distribution over seeds" \
  --spec-out examples/run-distribution/chart-boxplot.vl.json
```

`--aggregate` is not supported with boxplot; pass raw per-seed rows.

## Boxplot with raw points (hand-written spec)

`chart-points.vl.json` layers a translucent boxplot with per-seed points, jittered deterministically from the `seed` column so re-renders are reproducible. `infer` generates single-mark charts only, so this layered spec is hand-written.

```bash
vega-paper render examples/run-distribution/chart-points.vl.json \
  --theme paper-clean \
  --format svg \
  --out examples/run-distribution/output-points.svg
```

## Render

```bash
vega-paper render examples/run-distribution/chart-boxplot.vl.json \
  --theme paper-clean \
  --format svg \
  --out examples/run-distribution/output.svg
```

`chart-boxplot.vl.json` is regenerated with `bun run infer:run-distribution-boxplot` from the repo root. `chart-points.vl.json` is hand-written and NOT overwritten by `bun run infer:examples`. `output*.svg` files are local only.
```

- [ ] **Step 10: Update `skills/vega-paper/references/chart-selection.md`**

In the "Examples in this repo" table, insert directly after the `benchmark-heatmap` row added in Task 2:

```markdown
| [run-distribution/](../../../examples/run-distribution/) | `boxplot` | seed/run variability; hand-written boxplot + raw points overlay |
```

- [ ] **Step 11: Verify formatting and idempotent regeneration**

Run: `bun run check && bun run infer:examples && git status --short`
Expected: no biome errors; `infer:examples` exits 0 and regenerates all committed `chart*.vl.json` files byte-identically, so `git status --short` shows only the intentionally new/modified files from this task (no unexpected diffs in other examples).

Then run: `bun test packages/cli/test/examples.test.ts`
Expected: PASS (all tests).

- [ ] **Step 12: Commit**

```bash
git add examples/run-distribution package.json packages/cli/test/examples.test.ts skills/vega-paper/references/chart-selection.md
git commit -m "feat(examples): add run-distribution example with boxplot and raw-point overlay"
```

---

### Task 4: Gallery PNGs and example index updates (spec §12 Phase B items 4–5, §9.3)

**Files:**
- Modify: `scripts/render-gallery.ts` (`exampleJobs` array, lines 17–53)
- Create: `docs/assets/gallery/examples/ablation-bar.png`, `benchmark-heatmap.png`, `run-distribution.png` (via `bun run render:gallery`)
- Modify: `examples/README.md` (index table and gallery table)
- Test: `scripts/render-gallery.test.ts` (`EXAMPLE_PREVIEWS` array, lines 7–15 — this is the existing test that enumerates expected gallery PNGs; do NOT add a duplicate PNG-existence test to `packages/cli/test/examples.test.ts`)

**Interfaces:**
- Consumes: `examples/ablation-bar/chart.vl.json` (Task 1), `examples/benchmark-heatmap/chart-labeled.vl.json` (Task 2), `examples/run-distribution/chart-points.vl.json` (Task 3). The gallery renders the labeled/overlay variants because those are what make these recipes distinctive.

- [ ] **Step 1: Extend the gallery test (failing first)**

In `scripts/render-gallery.test.ts`, extend the `EXAMPLE_PREVIEWS` array. Replace:

```ts
const EXAMPLE_PREVIEWS = [
  "basic-line",
  "training-curve",
  "confusion-matrix",
  "faceted-training",
  "boxplot",
  "embedding-scatter",
  "custom-theme",
];
```

with:

```ts
const EXAMPLE_PREVIEWS = [
  "basic-line",
  "training-curve",
  "confusion-matrix",
  "faceted-training",
  "boxplot",
  "embedding-scatter",
  "ablation-bar",
  "benchmark-heatmap",
  "run-distribution",
  "custom-theme",
];
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bun test scripts/render-gallery.test.ts`
Expected: the `example PNGs exist` test FAILS (`expect(false).toBe(true)` — the three new PNGs are missing); the theme-PNG test still PASSES.

- [ ] **Step 3: Add gallery jobs to `scripts/render-gallery.ts`**

In the `exampleJobs` array, insert directly after the `embedding-scatter` job object and before the `custom-theme` job object:

```ts
  {
    spec: "examples/ablation-bar/chart.vl.json",
    theme: "paper-clean",
    out: `${GALLERY}/examples/ablation-bar.png`,
  },
  {
    spec: "examples/benchmark-heatmap/chart-labeled.vl.json",
    theme: "paper-clean",
    out: `${GALLERY}/examples/benchmark-heatmap.png`,
  },
  {
    spec: "examples/run-distribution/chart-points.vl.json",
    theme: "paper-clean",
    out: `${GALLERY}/examples/run-distribution.png`,
  },
```

- [ ] **Step 4: Render the gallery**

Run: `bun run render:gallery`
Expected: exit 0; logs `Rendering paper-clean -> docs/assets/gallery/examples/ablation-bar.png` (and benchmark-heatmap, run-distribution) among the existing jobs. The three new PNGs exist afterwards.

Then run: `git status --short docs/assets/gallery`
Expected: only the three new PNGs appear as untracked (`.meta.json` sidecars are gitignored). If any pre-existing PNG shows as modified, inspect it; when the image is visually unchanged, restore it with `git restore docs/assets/gallery/examples/<name>.png` (and `docs/assets/gallery/themes/<name>.png`) to keep the diff limited to the new files.

- [ ] **Step 5: Run test to verify it passes**

Run: `bun test scripts/render-gallery.test.ts`
Expected: PASS (all tests).

- [ ] **Step 6: Update `examples/README.md` index table**

Replace:

```markdown
| [embedding-scatter/](embedding-scatter/) | `infer` scatter; 2D embedding with `--color` |
| [theme-samples/](theme-samples/) | Same spec rendered with every built-in theme |
```

with:

```markdown
| [embedding-scatter/](embedding-scatter/) | `infer` scatter; 2D embedding with `--color` |
| [ablation-bar/](ablation-bar/) | `infer` bar; ablation and grouped benchmark comparison |
| [benchmark-heatmap/](benchmark-heatmap/) | `infer` heatmap; hand-written labeled variant (rect + text) |
| [run-distribution/](run-distribution/) | `infer` boxplot over seeds; hand-written boxplot + raw points overlay |
| [theme-samples/](theme-samples/) | Same spec rendered with every built-in theme |
```

- [ ] **Step 7: Update `examples/README.md` gallery table**

Replace:

```markdown
| [embedding-scatter/](embedding-scatter/) | ![embedding-scatter](../docs/assets/gallery/examples/embedding-scatter.png) |
| [custom-theme/](custom-theme/) | ![custom-theme](../docs/assets/gallery/examples/custom-theme.png) |
```

with:

```markdown
| [embedding-scatter/](embedding-scatter/) | ![embedding-scatter](../docs/assets/gallery/examples/embedding-scatter.png) |
| [ablation-bar/](ablation-bar/) | ![ablation-bar](../docs/assets/gallery/examples/ablation-bar.png) |
| [benchmark-heatmap/](benchmark-heatmap/) | ![benchmark-heatmap](../docs/assets/gallery/examples/benchmark-heatmap.png) |
| [run-distribution/](run-distribution/) | ![run-distribution](../docs/assets/gallery/examples/run-distribution.png) |
| [custom-theme/](custom-theme/) | ![custom-theme](../docs/assets/gallery/examples/custom-theme.png) |
```

(The benchmark-heatmap and run-distribution previews render the hand-written variants — `chart-labeled.vl.json` and `chart-points.vl.json` — which is fine given the table's "paper-clean unless noted" caption applies to the theme, not the spec file.)

- [ ] **Step 8: Full verification**

Run: `bun run check && bun test && bun run typecheck`
Expected: no biome errors; the full test suite PASSES; typecheck PASSES.

Note: root `README.md` needs no change — it links to `examples/README.md` without listing individual examples, and the §9.3 root-gallery split into "theme previews + ML recipes" is deferred until Phase C/D figure types exist.

- [ ] **Step 9: Commit**

```bash
git add scripts/render-gallery.ts scripts/render-gallery.test.ts docs/assets/gallery/examples/ablation-bar.png docs/assets/gallery/examples/benchmark-heatmap.png docs/assets/gallery/examples/run-distribution.png examples/README.md
git commit -m "docs(examples): add ML figure examples to gallery and index"
```

---

## Spec Ambiguities Resolved

1. **Grouped bar stacking (§7.2):** the spec's exact `infer` command for `grouped.csv` (bar + `--color`) produces *stacked* bars in Vega-Lite, since side-by-side grouping (`xOffset`) is a Phase C template concern. The plan follows the spec's command verbatim and documents the stacking caveat plus a `--facet` workaround in the README.
2. **§7.2 Template Extension:** excluded (Phase C), per the phase boundary; READMEs mention the planned `leaderboard-bar` template only in prose.
3. **§7.7 "existing boxplot docs are reframed":** §11 explicitly keeps `examples/boxplot/` as-is, so the reframing lives entirely in `examples/run-distribution/README.md`, which cross-links to `../boxplot/`. No boxplot files are touched.
4. **§7.7 file name:** §7.7 mentions `chart-with-points.vl.json` but §11's directory layout says `chart-points.vl.json`; the layout table wins.
5. **§9.3 root README gallery split:** deferred — the root README does not list individual examples, and the required ML-recipe gallery members (Pareto, scaling law, calibration, multipanel) are Phase C/D. Phase B item 4 ("Update examples README gallery") is satisfied by `examples/README.md`.
6. **Jitter determinism:** Vega-Lite's documented jitter pattern uses `random()`, which breaks byte-reproducible renders; the overlay spec derives jitter from the `seed` column instead (`datum.seed / 6`). Quantitative `xOffset` fields are valid Vega-Lite (the documented strip-plot jitter pattern); the presence of `xOffset` forces the shared x scale to a band scale, which matches the boxplot layer.
7. **Phase C overlap on `examples/benchmark-heatmap/` (§7.3 template path):** the Phase C plan's Task 10 writes its own `data.csv`/`README.md` and a template-generated `chart-labeled.vl.json` for the same folder. Ownership is resolved in Phase B's favor: this plan's `data.csv`, `README.md`, `chart.vl.json`, and hand-written `chart-labeled.vl.json` are the committed baseline; Phase C may only ADD files (e.g. its `template:benchmark-heatmap` script) and, if it regenerates `chart-labeled.vl.json` from the template, must adopt this plan's data, merge (not replace) the README, and update the 2-layer test added in Task 2 Step 1. The Phase C plan needs a corresponding revision (tracked outside this plan).
